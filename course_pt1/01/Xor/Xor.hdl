/**
 * @defgroup   XOR Exclusive-or
 *
 * @brief      This file implements Exclusive-or.
 *
 * @author     Vigneshrajarathinam
 * @date       2022
 */
 /**
  * Gate Info:
  * ----
  * out = A+B
  * 
  * Implementation:
  * ----
  * Since the basic gates are NAND and NOR,
  * constructing others with these will be
  * relevent.
  * 
  * out = Not(Not(A Or B))
  * 
  * since, AND = NOT(NOR(A,B))
  * 
  */
  CHIP Xor {
      IN a, b;
      OUT out;

      PARTS:
      
  }